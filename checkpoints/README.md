# Checkpoints
This directory stores the trained LoRA adapters for different datasets (LMSYS, Alpaca, Dolly).
Each sub-folder should contain:
- `adapter_config.json`
- `adapter_model.bin` (or `.safetensors`)